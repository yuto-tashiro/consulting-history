const cards=[...document.querySelectorAll('.chapter-card')];
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',String(x===button)));cards.forEach(c=>c.hidden=button.dataset.filter!=='all'&&c.dataset.category!==button.dataset.filter);document.querySelector('#filter-status').textContent=`${cards.filter(c=>!c.hidden).length}章を表示`;}));
const progress=document.querySelector('.reading-progress');if(progress){const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?Math.min(100,scrollY/max*100):0}%`;};addEventListener('scroll',update,{passive:true});addEventListener('resize',update);update();}
