const cards=[...document.querySelectorAll('.chapter-card')];
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',String(x===button)));cards.forEach(c=>c.hidden=button.dataset.filter!=='all'&&c.dataset.category!==button.dataset.filter);document.querySelector('#filter-status').textContent=`${cards.filter(c=>!c.hidden).length}章を表示`;}));
const questions=[['答えをつくるコストが下がるとき、何に価値が残るのだろう。','問いの設定、現場の理解、意思決定、実装。そのなかで、あなたの仕事が担っているものは何でしょうか。'],['調査や資料づくりをAIが担うとき、専門性はどう育つのだろう。','仕事を通して身についた判断力を思い出してみてください。その経験を、次の世代はどこで得られるでしょうか。'],['支援の成果は、顧客が自分で考えられるようになることだろうか。','外部の力を借りることと、自分たちの力を育てること。その二つを、どのように両立できるでしょうか。']];
let active=0;const notes=['','',''];const area=document.querySelector('#reflection');const status=document.querySelector('#save-status');
if(area){try{const saved=JSON.parse(localStorage.getItem('consulting-history-notes-v1')||'[]');notes.forEach((_,i)=>{notes[i]=typeof saved[i]==='string'?saved[i]:''});area.value=notes[0];}catch{status.textContent='このブラウザでは保存できません。書き出し機能をご利用ください。';}
area.addEventListener('input',()=>{notes[active]=area.value;try{localStorage.setItem('consulting-history-notes-v1',JSON.stringify(notes));status.textContent='このブラウザに保存しました。外部には送信されません。';}catch{status.textContent='保存できませんでした。メモを書き出して保存してください。';}});
document.querySelectorAll('[data-question]').forEach(button=>button.addEventListener('click',()=>{active=Number(button.dataset.question);document.querySelectorAll('[data-question]').forEach(x=>x.setAttribute('aria-pressed',String(x===button)));document.querySelector('#question-title').textContent=questions[active][0];document.querySelector('#question-hint').textContent=questions[active][1];area.value=notes[active];}));
document.querySelector('#download-note').addEventListener('click',()=>{const content='# コンサルティングの歴史 — 私のメモ\n\n'+questions.map((q,i)=>`## ${q[0]}\n\n${notes[i]||'（未記入）'}\n`).join('\n');const url=URL.createObjectURL(new Blob([content],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='consulting-history-notes.md';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);status.textContent='メモを書き出しました。';});}
const progress=document.querySelector('.reading-progress');if(progress){const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?Math.min(100,scrollY/max*100):0}%`;};addEventListener('scroll',update,{passive:true});addEventListener('resize',update);update();}
